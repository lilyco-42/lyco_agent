# Rust
lycore/target/
target/

# Python
__pycache__/
*.pyc

# 训练产物 (大文件, 不入库 —— 走 Release / 对象存储分发)
# 注意: 只写 models/*.tar 会漏掉 .gguf 量化模型(单文件 462M×2), 必须整目录忽略
models/
*.gguf
pack_merged.tar.gz

# 冒烟测试中间产物 (保留 pack 与最终视频作演示)
smoke/pack_tts*/
smoke/pack_tts2/
smoke/audio20*.wav
smoke/mix20.wav
smoke/m1.wav
smoke/e2_16k.wav
smoke/extracted.wav
smoke/t*.wav
smoke/p[1-4].mp4
smoke/dbg*
smoke/t_out*

# OS
.DS_Store
Thumbs.db
smoke/demo_pack/

# ARM 二进制由 CI 构建并挂到 release (releases/latest/download/lycore-aarch64),
# 不再入库 —— 避免 git 里的 stale 副本被误取 (曾是手工重编 3 次的漂移源)
dist/

# e2e 测试产物 (agent loop 生成, 非资产)
smoke/agent_*.html
smoke/agent_*.mp4
smoke/f*_v[0-9]*.png

# 会话调试知识包 (learn 出来的临时 pack, 非资产)
smoke/jj_pack/
smoke/lilyco_pack/

# 注: smoke/pack_merged/learning_queue.jsonl 是**真实队列样本**(非忽略) ——
#     lernen 的 NO_HIT 二次分类回归测试直接引用其中的 "队名"×7 / "起个标题"。
# 本地交叉编译 hack (CI 用 env 注入链接器, 不依赖 .cargo/config.toml)
lycore/.cargo/
lycore/zigcc-linker.sh
